mixin multilevel-menu(items, path)
  ul
    each item in items
      li
        a(href=`${path}/${item.slug}`) #{item.title}
        if item.children && item.children.length > 0
          +multilevel-menu(item.children, path)
