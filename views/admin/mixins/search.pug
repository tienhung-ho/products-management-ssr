mixin search(keyword)
    form(id='form-search')
        .input-group
            input(
                type='text'
                placeholder='Nhập từ khoá...'
                name='keyword'
                class='form-control'
                value=keyword
            )
            .input-group-append 
                button(
                    type='submit'
                    class='btn btn-success'
                ) Tìm

